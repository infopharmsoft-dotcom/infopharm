name: Generate Static Drug Pages

on:
  push:
    branches: [main]
  workflow_dispatch:  # تشغيل يدوي لو احتجت

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install fflate

      - name: Download drugs.json.gz from GitHub
        run: |
          curl -L "https://raw.githubusercontent.com/infopharmsoft-dotcom/Infopharm-Data1/main/drugs.json.gz" \
               -o drugs.json.gz

      - name: Generate static pages
        run: node generate.js

      - name: Generate Sitemap
        run: node sitemap.js

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true
